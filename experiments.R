start_time <- Sys.time()
devtools::install_local('./', force = TRUE)
library(PTTests)
experiment_lcd_roc_curves(simulation = 'paper')
experiment_lcd_compare_tests_roc(simulation = 'paper', results_file="lcd_roc_curves.Rdata")
experiment_lcd_compare_tests_auc(simulation = 'paper', results_file="lcd_auc_curves.Rdata")
experiment_sachs_lcd(plot_context = FALSE)
cat('Time elapsed (s): ', Sys.time() - start_time)
